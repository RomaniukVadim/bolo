# ===================================
# Filter Scope Definitions
# ===================================

scopes:
    active:
        label: Active
        type: switch
        conditions:
            - active = '0'
            - active = '1'
